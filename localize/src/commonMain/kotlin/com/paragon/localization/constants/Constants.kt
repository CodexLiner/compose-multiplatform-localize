package com.paragon.localization.constants

/** The key for storing the selected language code in preferences. */
const val SELECTED_LANGUAGE_CODE = "SELECTED_LANGUAGE_CODE"

/** The key for storing a map of all available languages. */
const val ALL_LANGUAGES_MAP = "ALL_LANGUAGES_MAP"

/** The key for storing the selected language map. */
const val SELECTED_LANGUAGE_MAP = "SELECTED_LANGUAGE_MAP"

/** Tag for language-related operations in the local storage. */
internal const val DV_LINGUAL_TAG = "LanguageLocal"

/** Base URL for the demo environment of Digivalet. */
internal const val BASE_URL = "https://demo.digivalet.com/"

/** Minimum interval time in milliseconds for periodic operations (15 minutes). */
internal const val MIN_PERIODIC_INTERVAL_MILLIS = 15 * 60 * 1000L // 15 minutes.

/** File extension for XML files. */
internal const val XML_EXTENSION = ".xml"
